# Python virtual environment (NEVER upload)
.venv/

# Large NASA C-MAPSS data (ignore to avoid bloat)
data/

# Ignore outputs folder EXCEPT the evaluation subfolder (plots/reports)
outputs/
!outputs/evaluation/  # Keep evaluation folder (plots + reports)
!outputs/evaluation/**/*  # Keep all files/subfolders inside evaluation/

# Ignore large/checkpoint files 
outputs/checkpoints/  # Ignore model checkpoints (large!)
outputs/metrics/      # Ignore raw metrics files (users generate their own)

# Python cache files
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/

# OS/IDE files
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# Ollama cache (if used)
.ollama/